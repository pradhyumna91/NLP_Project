language: hindi

model:
  name: "gpt-4o-mini"  # OpenAI API used for both generation and annotation

generation:
  stories_per_config: 3
  max_new_tokens: 1024
  temperature: 1.0
  top_p: 0.9

paths:
  prompts: "data/prompts"
  stories: "data/stories"
  extracted: "data/extracted"
